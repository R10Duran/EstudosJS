// Alterando estilos
const element = document.querySelector('body')

element.style.backgroundColor = '#f2f2f2' // aplico como se fosse o style em linha, no lugar do tracinho eu uso o método camelCase
console.log(element.style.backgroundColor) // posso também setar os valores para ver mais detalhes no console
