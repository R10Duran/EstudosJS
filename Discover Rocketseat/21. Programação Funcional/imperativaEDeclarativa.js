// Função que eleva o número ao quadrado

// Imperativo: Faça da seguinte forma (receita de bolo)
let number = 2

function square(){
  return number * number
}

number = square()
// Perceba que linha a linha fui informando como deve ser feita, ja na declarativa, vejamos o exemplo abaixo


// Declarativa: o que fazer e não como fazer
const square = n => n * n //aqui apenas criei uma const chamada square, que recebe um n(numero) e o recebe uma arrow function de número * numero (n*n), falei o que fazer na outra eu dei o passo a passo