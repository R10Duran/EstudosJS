// Navegando pelos elementos
// parentNode parentElement
const body = document.querySelector('body')

console.log(body.parentNode) //parentNode ou parentElement--> de quem é o nó pai, no caso o body é filho do HTML, pega-se então o nó pai que é o HTML